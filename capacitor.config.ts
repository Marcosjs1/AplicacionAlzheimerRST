import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marcos.RSTMindHealth',
  appName: 'FullSaludAlzheimer',
  webDir: 'dist'
};

export default config;
